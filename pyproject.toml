[project]
name = "apple-doc-scraper"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "bs4>=0.0.2",
    "click>=8.1.8",
    "lxml>=5.3.2",
    "playwright>=1.51.0",
    "requests>=2.32.3",
    "rich>=14.0.0",
]

[project.scripts]
apple-doc-scraper = "src.scraper:main"